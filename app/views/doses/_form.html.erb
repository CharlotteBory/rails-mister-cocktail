<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.input :description, label: "Quantity (ex. 4cl, 6oz, 1 leaf...)" %>
<%= f.association :ingredient %>
<%= f.button :submit, class: "btn btn-outline-primary" %>
<% end %>
